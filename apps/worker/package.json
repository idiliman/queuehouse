{
  "name": "@queuehouse/worker",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/worker.ts",
    "start": "bun run src/worker.ts",
    "build": "bun build src/worker.ts --outdir dist --target bun",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@queuehouse/core": "workspace:*",
    "bullmq": "^5.40.0",
    "ioredis": "^5.6.0",
    "prom-client": "^15.1.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.3"
  }
}
